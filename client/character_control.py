import client.character_action as character_action


class CharacterControl:


    def __init__(self) -> None:
        self.last_character_data = get_character_data

    def fight_chickens(str):
        # store map data globally somewhere. Use it to know where to move.
        character_action.move()